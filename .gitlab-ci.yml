image: mcr.microsoft.com/dotnet/sdk:9.0

workflow:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push"'
    - when: never

stages:
  - build
  - test

variables:
  DOTNET_CLI_TELEMETRY_OPTOUT: "1"
  DOTNET_NOLOGO: "true"

before_script:
  - dotnet restore src/ArgParser.sln

build:
  stage: build
  script:
    - dotnet build src/ArgParser.sln --configuration Release --no-restore
  artifacts:
    paths:
      - src/ArgParser/bin/
      - src/ArgParser/obj/
      - src/tests/APITests/bin/
      - src/tests/APITests/obj/
      - src/tests/ArgParserTests/bin/
      - src/tests/ArgParserTests/obj/

test:
  stage: test
  needs:
    - job: build
      artifacts: true
  script:
    - dotnet test src/ArgParser.sln --configuration Release --no-build --no-restore